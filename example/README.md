# React-krpano-hooks Example

This is an example application for the `react-krpano-hooks` library, built with Vite.

## Development

```bash
# Install dependencies
yarn install

# Start development server
yarn dev
```

## Production

```bash
# Build for production
yarn build

# Preview production build
yarn preview

# Deploy to GitHub Pages
yarn deploy
```
